# モバイルアプリ技術要件定義書への適合表

対象：夏休みダイヤ 2026（v4.0）／確認日：2026年7月30日

## 4章｜モバイルアプリとしての全体要件

| 要件 | 実装 | 状態 |
|---|---|---|
| 実行基盤：PWA、サーバー不要・APIキー不要 | 素のHTML/CSS/JS。全データをファイル内に静的生成 | ✅ |
| オフライン対応：Service Workerでアプリシェルをキャッシュ | `sw.js`（CACHE `summer-dia-v4`）にHTML・manifest・アイコン4点を登録 | ✅ |
| インストール性：manifest必須、`display: standalone` | `manifest.webmanifest` 配置済み、standalone指定 | ✅ |
| データ永続化：端末内保存＋書き出し・読み込み | localStorage（アプリ時）／window.storage（プレビュー時）＋JSON書き出し・読み込みボタン | ✅ |
| 配信：HTTPS必須 | GitHub Pages または XREA（SSL有効化）で配信 | ⚠️ 配置時に要確認 |
| 両OS実機検証 | Android実機で要確認。iOSは端末があれば確認 | ⚠️ 未実施 |

## 5章｜アイコンの表示・ダウンロード機能

### アイコンセット（5.1 A）

| ファイル | サイズ | 形式 | 宣言箇所 | 状態 |
|---|---|---|---|---|
| `icon.svg` | any | SVG | `<link rel="icon">`／manifest | ✅ |
| `icon-180.png` | 180×180 | PNG | `<link rel="apple-touch-icon">` | ✅ |
| `icon-192.png` | 192×192 | PNG | manifest（`purpose: any`） | ✅ |
| `icon-512.png` | 512×512 | PNG | manifest（`any` と `maskable` の2エントリ） | ✅ |

- **maskableセーフゾーン**：`SAFE = 0.70` としてモチーフを中央70%に収めています。Androidが丸型・角丸にトリミングしても縦帯は欠けません。
- **テーマカラーの統一**：`theme_color` / `background_color` をともに `#0E1319` とし、アイコン背景・起動スプラッシュ・ステータスバーを一致させています。
- **合計サイズ 8.7KB**（SVG 0.5KB + PNG 3点 8.2KB）。要件の「数KB〜十数KB」に収まっています。

### 生成・書き出し機能（5.1 B）

| # | 要件 | 実装 | 状態 |
|---|---|---|---|
| B-1 | マスターから全サイズを自動生成、依存ライブラリなし | `gen-icons.cjs`。標準モジュール `zlib` のみ使用し、PNGエンコード・CRC32・角丸描画・4×4スーパーサンプリングを自前実装 | ✅ |
| B-2 | 決まったディレクトリに出力しコミット対象とする | スクリプトと同ディレクトリに出力 | ✅ |
| B-3 | 生成後に表示確認 | 512pxを目視確認済み | ✅ |
| B-4 | ホーム画面追加の導線をアプリ内に明示 | ヘッダー直下のインストールバー。Androidは `beforeinstallprompt` を捕捉、iOSは共有→ホーム画面に追加の手順をトーストで案内 | ✅ |
| B-5 | インストール済み判定で二重案内をしない | `matchMedia('(display-mode: standalone)')` と `navigator.standalone` で判定し、インストール済みならバーを出さない | ✅ |

### 非機能要件（5.2）

| 要件 | 実装 | 状態 |
|---|---|---|
| 無料・オフライン生成（外部API不要） | Node.js標準機能のみ。ネットワーク不要 | ✅ |
| 再現性（決定的な生成処理） | 乱数を使わず、座標はすべて size 比例で算出。何度実行しても同一バイト列 | ✅ |
| 軽量性 | 合計8.7KB | ✅ |

## 5.4｜検証手順（配置後に実施してください）

| # | 検証項目 | 方法 | 状態 |
|---|---|---|---|
| 1 | 全アイコンがHTTP 200で取得できる | 配信後にURL直打ちで確認 | ⬜ 未実施 |
| 2 | manifestの`icons[]`が正しいtype/sizes/purposeを持つ | manifestをブラウザで開いて確認 | ✅ 記述は準拠 |
| 3 | iOSで「ホーム画面に追加」時にアイコンが正しく出る | iOS実機のSafariで確認 | ⬜ 未実施 |
| 4 | Androidでインストールボタンが機能し、maskable表示が崩れない | Android実機のChromeで確認 | ⬜ 未実施 |
| 5 | Service Workerがアイコンをキャッシュし、オフラインでも表示される | 機内モードで起動して確認 | ⬜ 未実施 |

⚠️ **1・3・4・5は実機とHTTPS配信が必要なため、この場では検証できません。** GitHub Pagesに配置したあと、上の5項目を順に確認してください。特に3は、PNGでない場合に白紙アイコンになる典型的な失敗ポイントです。

## 6章｜引き継ぎチェックリスト

- [x] マスターアイコンのデザインファイルが手元にある（`icon.svg`）
- [x] `icon.svg` / `icon-180.png` / `icon-192.png` / `icon-512.png` が生成・配置されている
- [x] `manifest.webmanifest` の `icons[]` に4件正しく宣言されている
- [x] `index.html` に `apple-touch-icon` と `rel="icon"` が設置されている
- [x] インストールボタン（またはヒント表示）が実装されている
- [x] Service Worker のキャッシュ対象にアイコン一式が含まれている
- [ ] iOS実機・Android実機の両方で表示・インストールを確認済み ← **残タスク**

## ファイル一覧（GitHub Pagesには全7ファイルを配置）

| ファイル | 役割 |
|---|---|
| `index.html` | アプリ本体（カレンダー・週表示・目標パネル・インストール導線・バックアップ） |
| `manifest.webmanifest` | アイコン宣言を含むPWAマニフェスト |
| `sw.js` | アプリシェルのキャッシュ定義 |
| `icon.svg` | マスターアイコン（ベクター） |
| `icon-180.png` / `icon-192.png` / `icon-512.png` | 生成されたアイコン |
| `gen-icons.cjs` | アイコン生成スクリプト（配信不要。リポジトリに置くだけ） |

`gen-icons.cjs` はブラウザから読まれないので、配信対象に含めても害はありません。デザインを変えたくなったら、ファイル上部の `BARS` の色と高さを書き換えて `node gen-icons.cjs` を実行し直してください。
